{
  "status": true,
  "data": {
    "id": "{{#? promotion.id}}",
    "name": "{{#? promotion.name}}",
    "description": "{{#? promotion.description}}",
    "urlImage": "{{#? promotion.urlImage}}"
  }
}