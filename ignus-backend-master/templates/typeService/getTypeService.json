{
  "status": true,
  "data": {
      "id": "{{#? typeService.id}}",
      "name": "{{#? typeService.name}}",
      "description": "{{#? typeService.description}}",
      "urlImage": "{{#? typeService.urlImage}}",
      "offeringProperty": "{{typeService.offeringProperty}}",
      "requirements" : {
        "{{#each typeService.Requirements}}": {
          "id": "{{#? this.id}}",
          "name": "{{#? this.name}}",
          "description": "{{#? this.description}}"
        }
      },
      "activities" : {
        "{{#each typeService.Activities}}": {
          "id": "{{#? this.id}}",
          "name": "{{#? this.name}}",
          "description": "{{#? this.description}}"
        }
      }
    }
}