{
  "status": true,
  "data": {
    "id": "{{#? calification.id}}",
    "TransactionId": "{{#? calification.TransactionId}}",
    "TypeCalification" : {
      "id": "{{#? calification.TypeCalification.id}}",
      "name": "{{#? calification.TypeCalification.name}}",
      "description": "{{#? calification.TypeCalification.description}}"
    },
    "QualificationCriteria" : {
        "id": "{{#? calification.QualificationCriterium.id}}",
        "name": "{{#? calification.QualificationCriterium.name}}",
        "description": "{{#? calification.QualificationCriterium.description}}"
      }
  }
}