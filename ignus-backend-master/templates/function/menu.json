{
  "status": true,
  "data": {
    "{{#each functions}}": {
      "id": "{{#? this.id}}",
      "name": "{{#? this.name}}",
      "link": "{{#? this.link}}",
      "icon": "{{#? this.icon}}",
      "sub" : {
        "{{#each this.children}}": {
          "id": "{{#? this.id}}",
          "name": "{{#? this.name}}",
          "link": "{{#? this.link}}",
          "icon": "{{#? this.icon}}",
          "sub" : {
            "{{#each this.children}}": {
              "id": "{{#? this.id}}",
              "name": "{{#? this.name}}",
              "link": "{{#? this.link}}",
              "icon": "{{#? this.icon}}"
            }
          }
        }
      }
    }
  }
}